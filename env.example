# Root env.example for Maku Travel
# Copy this file to .env.local (frontend) and fill in values as needed.

# Frontend (Vite) - prefixed with VITE_
VITE_SUPABASE_URL=https://<your-project>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-key>

# Supabase edge functions - service role and providers (do NOT commit real secrets)
SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# Provider API placeholders
AMADEUS_CLIENT_ID=<ama-client-id>
AMADEUS_CLIENT_SECRET=<ama-client-secret>
HOTELBEDS_API_KEY=<hotelbeds-key>
HOTELBEDS_API_SECRET=<hotelbeds-secret>
SABRE_CLIENT_ID=<sabre-client-id>
SABRE_CLIENT_SECRET=<sabre-client-secret>
STRIPE_SECRET_KEY=<stripe-secret>

# FastAPI action (maku-supabase-action/server)
APP_BASE_URL=http://localhost:3333
ALLOWED_ORIGINS=http://localhost:3333

# Optional: change ports for local supabase start if needed
# SUPABASE_LOCAL_PORTS can be defined in supabase/config.toml; keep defaults unless you know what you are doing
