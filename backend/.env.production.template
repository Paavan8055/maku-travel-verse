# BACKEND PRODUCTION ENVIRONMENT TEMPLATE
# Configure these in Emergent Deployment Settings
# DO NOT commit actual production values to GitHub!

# Database Configuration
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/maku_production
DB_NAME=maku_production

# Supabase Configuration
SUPABASE_URL=https://iomeddeasarntjhqzndu.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_production_service_role_key_here
SUPABASE_ANON_KEY=your_production_anon_key_here

# CORS - CRITICAL: Update with your actual Netlify domain
CORS_ORIGINS=https://your-netlify-site.netlify.app,https://maku.travel

# Provider API Keys - PRODUCTION VALUES
# Sabre GDS
SABRE_CLIENT_ID=V1:production_username:pcc:AA
SABRE_CLIENT_SECRET=production_base64_password
SABRE_API_BASE=https://api.sabre.com
SABRE_ENV=production

# HotelBeds
HOTELBEDS_API_KEY=production_hotelbeds_api_key
HOTELBEDS_API_SECRET=production_hotelbeds_secret
HOTELBEDS_API_BASE=https://api.hotelbeds.com
HOTELBEDS_ENV=production

# Amadeus Self-Service APIs
AMADEUS_API_KEY=production_amadeus_api_key
AMADEUS_API_SECRET=production_amadeus_secret
AMADEUS_API_BASE=https://api.amadeus.com
AMADEUS_ENV=production

# Viator
VIATOR_API_KEY=production_viator_api_key
VIATOR_API_BASE=https://api.viator.com

# GetYourGuide
GETYOURGUIDE_API_KEY=production_getyourguide_key
GETYOURGUIDE_API_SECRET=production_getyourguide_secret

# Expedia TAAP
EXPEDIA_API_KEY=production_expedia_key
EXPEDIA_API_SECRET=production_expedia_secret

# OpenAI ChatGPT Pro
OPENAI_API_KEY=sk-prod-your-production-openai-key
OPENAI_MODEL=gpt-4o
SMART_DREAMS_MODEL=gpt-4o
TRAVEL_DNA_MODEL=o1
RECOMMENDATIONS_MODEL=gpt-4o-mini

# Emergent LLM Key (if using)
EMERGENT_LLM_KEY=sk-emergent-production-key

# Provider Configuration
PROVIDER_ROTATION_ENABLED=true
PROVIDER_ECO_PRIORITY=true
PROVIDER_LOCAL_FIRST=true
PROVIDER_HEALTH_CHECK_INTERVAL=300
PROVIDER_MAX_RETRIES=3
PROVIDER_TIMEOUT_MS=30000

# Security
JWT_SECRET=generate_strong_random_secret_here_min_32_chars
ENCRYPTION_KEY=generate_strong_random_encryption_key_here
SECRET_KEY=another_random_secret_for_sessions

# Environment
ENVIRONMENT=production
USE_EMERGENT_AI=true
USE_FREE_AI=false
DEVELOPMENT_MODE=false

# Cost Management
DAILY_COST_THRESHOLD=100
USER_DAILY_THRESHOLD=10

# Blockchain (if using)
BLOCKCHAIN_MODE=production
POLYGON_RPC_URL=https://polygon-rpc.com
MAKU_TOKEN_ADDRESS=0x_production_token_address
MAKU_NFT_ADDRESS=0x_production_nft_address
BLOCKCHAIN_PRIVATE_KEY=production_private_key_encrypted

# Error Tracking
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Email (if configured)
SENDGRID_API_KEY=production_sendgrid_key
FROM_EMAIL=noreply@maku.travel

# Rate Limiting
RATE_LIMIT_PER_MINUTE=100
RATE_LIMIT_PER_HOUR=1000

# Session
SESSION_SECRET=random_session_secret_min_32_chars

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# IMPORTANT SECURITY NOTES:
# 1. Generate new secrets for production (don't reuse development keys)
# 2. Use strong random values (32+ characters for secrets)
# 3. Never commit this file with actual values
# 4. Rotate keys every 90 days
# 5. Use Supabase Vault for provider API keys
# 6. Enable 2FA on all provider accounts
# 7. Monitor error logs for suspicious activity
# 8. Set up alerts for unusual API usage
